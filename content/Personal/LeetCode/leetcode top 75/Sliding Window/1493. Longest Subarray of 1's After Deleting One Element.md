---
publish: true
created: 2026-02-07T01:09:34.348+01:00
modified: 2026-02-07T01:20:53.042+01:00
cssclasses: ""
---

## Description

<p>Given a binary array <code>nums</code>, you should delete one element from it.</p>
<p>Return <em>the size of the longest non-empty subarray containing only </em><code>1</code><em>&#39;s in the resulting array</em>. Return <code>0</code> if there is no such subarray.</p>
<p><strong class="example">Example 1:</strong></p>
<pre>
<strong>Input:</strong> nums = [1,1,0,1]
<strong>Output:</strong> 3
<strong>Explanation:</strong> After deleting the number in position 2, [1,1,1] contains 3 numbers with value of 1&#39;s.
</pre>
<p><strong class="example">Example 2:</strong></p>
<pre>
<strong>Input:</strong> nums = [0,1,1,1,0,1,1,0,1]
<strong>Output:</strong> 5
<strong>Explanation:</strong> After deleting the number in position 4, [0,1,1,1,1,1,0,1] longest subarray with value of 1&#39;s is [1,1,1,1,1].
</pre>
<p><strong class="example">Example 3:</strong></p>
<pre>
<strong>Input:</strong> nums = [1,1,1]
<strong>Output:</strong> 2
<strong>Explanation:</strong> You must delete one element.
</pre>
<p><strong>Constraints:</strong></p>
<ul>
	<li><code>1 &lt;= nums.length &lt;= 10<sup>5</sup></code></li>
	<li><code>nums[i]</code> is either <code>0</code> or <code>1</code>.</li>
</ul>

---
## Solution

*this is basically finding the longest sequence with either one zero max and -1

```python

def longest_subarray(v: []):
    left, zero_count, max_len = 0, 0, 0

    for right in range(len(v)):
        if v[right] == 0:
            zero_count += 1

        while zero_count > 1:
            if v[left] == 0:
                zero_count -= 1
            left += 1

        max_len = max(max_len, right - left)

    return max_len
```

```python
nums = [1,1,0,1]
max_v = longest_subarray(nums)
print(max_v)
```

```python
nums = [0,1,1,1,0,1,1,0,1]
max_v = longest_subarray(nums)
print(max_v)
```

```python
nums = [1,1,1]
max_v = longest_subarray(nums)
print(max_v)
```