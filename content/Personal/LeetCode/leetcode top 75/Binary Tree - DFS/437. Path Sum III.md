---
publish: true
created: 2026-01-14T15:25:37.184+01:00
modified: 2026-01-14T16:44:31.691+01:00
cssclasses: ""
---

## Description

<p>Given the <code>root</code> of a binary tree and an integer <code>targetSum</code>, return <em>the number of paths where the sum of the values&nbsp;along the path equals</em>&nbsp;<code>targetSum</code>.</p>
<p>The path does not need to start or end at the root or a leaf, but it must go downwards (i.e., traveling only from parent nodes to child nodes).</p>
<p><strong class="example">Example 1:</strong></p>
<img alt="" src="../../../../Assets/LetCode Assets/path-sum-iii_0.jpg" style="width: 450px; height: 386px;" />
<pre>
<strong>Input:</strong> root = [10,5,-3,3,2,null,11,3,-2,null,1], targetSum = 8
<strong>Output:</strong> 3
<strong>Explanation:</strong> The paths that sum to 8 are shown.
</pre>
<p><strong class="example">Example 2:</strong></p>
<pre>
<strong>Input:</strong> root = [5,4,8,11,null,13,4,7,2,null,null,5,1], targetSum = 22
<strong>Output:</strong> 3
</pre>
<p><strong>Constraints:</strong></p>
<ul>
	<li>The number of nodes in the tree is in the range <code>[0, 1000]</code>.</li>
	<li><code>-10<sup>9</sup> &lt;= Node.val &lt;= 10<sup>9</sup></code></li>
	<li><code>-1000 &lt;= targetSum &lt;= 1000</code></li>
</ul>

---

**brute force**

```python
from algos.binarytree import Node

count = [0]

def dfs(node: Node, targetSum: int):
	
	if not node:
		return
		
	test(node, targetSum)
	dfs(node.left, targetSum)
	dfs(node.right, targetSum)
	return count[0]


def test(node: Node, targetSum:int):

	if not node:
		return 0 
	
	if targetSum == node.val:
		count[0] += 1
	
	test(node.left, targetSum - node.val)
	test(node.right, targetSum - node.val)	
```

```python
root = [10,5,-3,3,2,None,11,3,-2,None,1]
targetSum = 8

root = Node.create_binarytree(root)
c = dfs(root, targetSum)
print(c)
count = [0]
```

```python
root = [5,4,8,11,None,13,4,7,2,None,None,5,1]
targetSum = 22

root = Node.create_binarytree(root)
c = dfs(root, targetSum)
count = [0]
print(c)
```


**optimized solution**


```python
from collections import defaultdict

cache = defaultdict(int)
cache[0] = 1
result = [0]

def dfs(node:Node, target: int, current_sum: int):

    if not node:
        return

    current_sum += node.val
    expected_sum = current_sum - target
    
    result[0] += cache[expected_sum]
    cache[current_sum] += 1
    
    dfs(node.left, target, current_sum)
    dfs(node.right, target, current_sum)
    cache[current_sum] -= 1
```

```python
root = [10,5,-3,3,2,None,11,3,-2,None,1]
targetSum = 8

root = Node.create_binarytree(root)
dfs(root, targetSum, 0)
print(result[0])
result = [0]
```

```python
root = [5,4,8,11,None,13,4,7,2,None,None,5,1]
targetSum = 22

root = Node.create_binarytree(root)
dfs(root, targetSum, 0)
print(result[0])

```