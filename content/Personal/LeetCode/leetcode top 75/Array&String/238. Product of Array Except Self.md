---
publish: true
created: 2026-01-14T15:25:37.186+01:00
modified: 2026-02-03T21:57:49.651+01:00
cssclasses: ""
---

## Description

<p>Given an integer array <code>nums</code>, return <em>an array</em> <code>answer</code> <em>such that</em> <code>answer[i]</code> <em>is equal to the product of all the elements of</em> <code>nums</code> <em>except</em> <code>nums[i]</code>.</p>
<p>The product of any prefix or suffix of <code>nums</code> is <strong>guaranteed</strong> to fit in a <strong>32-bit</strong> integer.</p>
<p>You must write an algorithm that runs in&nbsp;<code>O(n)</code>&nbsp;time and without using the division operation.</p>
<p><strong class="example">Example 1:</strong></p>
<pre><strong>Input:</strong> nums = [1,2,3,4]
<strong>Output:</strong> [24,12,8,6]
</pre><p><strong class="example">Example 2:</strong></p>
<pre><strong>Input:</strong> nums = [-1,1,0,-3,3]
<strong>Output:</strong> [0,0,9,0,0]
</pre>
<p><strong>Constraints:</strong></p>
<ul>
	<li><code>2 &lt;= nums.length &lt;= 10<sup>5</sup></code></li>
	<li><code>-30 &lt;= nums[i] &lt;= 30</code></li>
	<li>The input is generated such that <code>answer[i]</code> is <strong>guaranteed</strong> to fit in a <strong>32-bit</strong> integer.</li>
</ul>
<p><strong>Follow up:</strong>&nbsp;Can you solve the problem in <code>O(1)</code>&nbsp;extra&nbsp;space complexity? (The output array <strong>does not</strong> count as extra space for space complexity analysis.)</p>

---

## Solution

The problem specifies that we cannot use division operation. That would be having product of all elements and dividing it by self. Very easy solution indeed.

First iteration to this problem is to store the product of the left and the right on different arrays. And sum them later on. From that point I will iterate to 0(1) solution.

nums = [1,2,3,4]

for the array above we can 

right_products = [24, 12, 4, 1]
left_products = [1, 1, 2, 6]

now we have two arrays that we can product to get the final result:

product_except_self = right_products *  left_products => [24, 12, 8, 6]

Let's write the algorithm

```python
nums = [1, 2, 3, 4]

def product_of_array_except_self(nums):
    n = len(nums)
    left_products = [1] * n
    right_products = [1] * n
    products = [1] * n
    
    left = 1
    for i in range(1, n):
        left_products[i] = nums[i - 1] * left
        left *= nums[i - 1]
        
    right = 1
    for j in range(n - 2, -1, -1):
        right_products[j] = nums[j + 1] * right
        right *= nums[j + 1]
    
    for i in range(n):
        products[i] = right_products[i] * left_products[i]
    return products
    
print(product_of_array_except_self(nums))
```

In the solution above we hold reference to 3 different arrays of length n. So the space complexity is O(3n) => 0(n)

Unfortunately I cannot figure out solving this question in O(1) space If we do count the return array as consumed space.

If so we can reduce this to use only one array by just storing the products.

Think of this example again:


    nums = [1,2,3,4]
    left_products = [1, 1, 2, 6]
    right_products = [24, 12, 4, 1]
    products = [24, 12, 8, 6]

Once we have the left products after the first loop, we can loop over it and change it in-place to generate products array. 

    nums[1] * left_products[1]

Implementation:

```python
nums = [1, 2, 3, 4]

def product_of_array_except_self(nums):
    n = len(nums)
    products = [1] * n
    
    left = 1
    for i in range(1, n):
        products[i] = nums[i - 1] * left
        left *= nums[i - 1]
        
    right = 1
    for j in range(n - 1, -1, -1):
        products[j] *= right
        right *= nums[j]
    
    return products
    
print(product_of_array_except_self(nums))
```

With the above code we modified only return array. Idk if there are any better solution than this one since we will need reference to nums either way.